version: 2.1

orbs:
  ruby: circleci/ruby@1.1.0

jobs:
  test:
    docker:
      - image: cimg/ruby:2.7-node
    steps:
      - checkout
      - ruby/install-deps:
          bundler-version: 2.1.4
      - ruby/rspec-test

workflows:
  ruby-test:
    jobs:
      - test
